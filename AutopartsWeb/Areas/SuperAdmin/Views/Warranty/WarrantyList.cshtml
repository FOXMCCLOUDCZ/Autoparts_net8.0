@model List<WarrantyListVM>
@{
    ViewData["Title"] = "WarrantyList";
    Layout = "~/Areas/SuperAdmin/Views/Shared/_Layout.cshtml";
}

<div>
    <h5 class="card-title">Záruční lhůty</h5>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-area="SuperAdmin" asp-controller="Dashboard" asp-action="Index">Dashboard</a></li>
            <li class="breadcrumb-item active"><a asp-area="SuperAdmin" asp-controller="Warranty" asp-action="WarrantyList">Záruční lhůty</a></li>
        </ol>
    </nav>
</div>


<div class="d-grid gap-2 mt-2 mb-2">
    <a asp-area="SuperAdmin" asp-controller="Warranty" asp-action="WarrantyCreate" class="btn btn-success btn-sm px-2"><i class="bi bi-plus-lg"></i></a>
</div>

<div class="card">
    <div class="card-body">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th class="text-center align-middle" scope="col">Název</th>
                    <th class="text-center align-middle" scope="col">Vytvořeno<br />Editováno</th>
                    <th class="text-center align-middle" scope="col">Aktivní<br />neaktivní</th>
                    <th class="text-center align-middle" scope="col">Akce</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center align-middle">@item.WarrantyPeriod</td>
                        <td class="text-center align-middle">@item.CreateDate.ToString("d")<br />@item.UpdateDate?.ToString("d")</td>
                        @if (item.IsActive == false)
                        {
                            <td class="text-center align-middle">
                                <div class="btn btn-danger btn-sm">
                                    <i class="bi bi-x"></i>
                                </div>
                            </td>
                        }
                        else
                        {
                            <td class="text-center align-middle">
                                <div class="btn btn-success btn-sm">
                                    <i class="bi bi-check-lg"></i>
                                </div>
                            </td>
                        }
                        <td class="text-center align-middle">
                            <a asp-area="SuperAdmin" asp-controller="Warranty" asp-action="WarrantyEdit" asp-route-id="@item.Id" data-id="@item.Id" class="btn btn-warning btn-sm"><i class="bi bi-pencil-square"></i></a>
                            <a asp-area="SuperAdmin" asp-controller="Warranty" asp-action="WarrantyDelete" asp-route-id="@item.Id" data-id="@item.Id" class="btn btn-danger btn-sm"><i class="bi bi-trash3-fill"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>